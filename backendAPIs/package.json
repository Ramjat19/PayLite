{
  "name": "paylite-mock-api",
  "version": "1.0.0",
  "private": true,
  "description": "In-memory mock API for the PayLite Flutter capstone.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
